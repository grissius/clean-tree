import { postMemoryRepository } from "../../infra/repositories/post-repo.memory"
import { Post } from "../entities/post"

it('List posts', async () => {
    // const INPUT: ListPostsInput = {}
    // await Promise.all(Array(100).fill(0).map((_, i) => postMemoryRepository.createPost(Post.NewPost({ userId: '', title: `title-${i}`, text: `text-${i}` }))))
    // const result = await ListPosts(INPUT)
})
